# SS7 - Grid World Attack Simulator
This folder contains a grid world simulation to test algorithms for anomaly detection in SS7 traffic.

* Install with ``npm install``.

* Run with ``npm start``.

* Build with ``npm run build-prod``.